info:
  name: Create a tag
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/tags
  body:
    type: json
    data: |-
      {
        "name": "work"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
