info:
  name: Get one note
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:3000/notes/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
