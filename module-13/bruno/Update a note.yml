info:
  name: Update a note
  type: http
  seq: 10

http:
  method: PUT
  url: http://localhost:3000/notes/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
