info:
  name: Get one tag
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/tags/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
