info:
  name: Create a note
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
