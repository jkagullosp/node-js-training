info:
  name: Delete board
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/boards/{{boardId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
