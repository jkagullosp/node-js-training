info:
  name: Get board
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/boards/{{boardId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
