info:
  name: Test Unknown Route
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/unknown-route"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
