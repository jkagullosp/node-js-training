info:
  name: Delete task
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/tasks/{{taskId}}"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
