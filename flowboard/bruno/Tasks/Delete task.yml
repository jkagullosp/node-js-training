info:
  name: Delete task
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/tasks/{{taskId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
