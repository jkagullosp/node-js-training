info:
  name: Get task
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/tasks/{{taskId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
