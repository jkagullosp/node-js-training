info:
  name: Get server readiness
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/ready"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
